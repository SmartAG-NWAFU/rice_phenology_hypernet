# Artifacts Directory

This directory is intentionally empty in the public repository.

Runtime outputs are written here:

- `features/`: modeling tables and regional-grid feature inputs
- `models/`: deployment model artifacts and checkpoints
- `eval/`: experiment predictions, metrics, diagnostics, and run manifests
- `figures/`: generated figures
- `tables/`: generated tables

All generated artifacts are ignored by git.
